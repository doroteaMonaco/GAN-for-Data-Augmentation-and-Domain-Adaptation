experiment_name: gan_augmented

dataset:
  type: augmented
  synthetic_ratio: 1.0 
  #synthetic_ratio = #synthetic malignant samples / #real malignant samples 



model:
  name: resnet50
  pretrained: true

training:
  epochs: 50
  batch_size: 32
  lr: 0.0001